{
  "name": "memory",
  "version": "0.1.0",
  "type": "mcp",
  "description": "Python MCP memory server: promote facts to long-term memory, list/review memories, manage profile memory files, and persist channel summaries (save_summary).",
  "config_schema": [
    {
      "key": "database_url",
      "label": "Database URL",
      "type": "string",
      "default": "$env:DATABASE_URL",
      "description": "PostgreSQL connection URL for memory storage"
    },
    {
      "key": "omni_dir",
      "label": "OMNI_DIR",
      "type": "string",
      "default": "$env:OMNI_DIR",
      "description": "Data directory for wiki memory files"
    }
  ]
}
