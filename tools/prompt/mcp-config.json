{
  "servers": [
    {
      "name": "prompt",
      "transport": "stdio",
      "command": "python3",
      "args": ["server.py"],
      "env": {"OMNI_DIR": "${OMNI_DIR}"},
      "timeout_secs": 900,
      "max_retries": 1,
      "pool_size": 3,
      "allowed_tools": ["*"]
    }
  ]
}
